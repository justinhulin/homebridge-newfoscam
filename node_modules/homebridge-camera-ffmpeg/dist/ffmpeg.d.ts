/// <reference types="node" />
import { StreamRequestCallback } from 'homebridge';
import { Writable } from 'stream';
import { Logger } from './logger';
import { StreamingDelegate } from './streamingDelegate';
export declare class FfmpegProcess {
    private readonly process;
    constructor(cameraName: string, sessionId: string, videoProcessor: string, ffmpegArgs: string, log: Logger, debug: boolean, delegate: StreamingDelegate, callback?: StreamRequestCallback);
    stop(): void;
    getStdin(): Writable | null;
}
//# sourceMappingURL=ffmpeg.d.ts.map